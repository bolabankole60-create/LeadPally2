'use client';
import {FormEvent,useState} from 'react'; import {useRouter} from 'next/navigation'; import api from '@/lib/api';
export default function NewTeamPage(){const router=useRouter();const[name,setName]=useState('');const[error,setError]=useState('');const[loading,setLoading]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);setError('');try{await api.post('/v1/teams',{name});router.push('/dashboard')}catch{setError('Could not create team. Please try again.')}finally{setLoading(false)}}return <main className="flex min-h-screen items-center justify-center bg-slate-50 px-4"><form onSubmit={submit} className="w-full max-w-md rounded-2xl bg-white p-8 shadow-sm"><h1 className="text-2xl font-bold text-slate-900">Create Team</h1><p className="mt-2 text-sm text-slate-500">Set up your company or workspace.</p>{error&&<p className="mt-4 rounded-lg bg-red-50 p-3 text-sm text-red-600">{error}</p>}<label className="mt-6 block text-sm font-medium text-slate-700">Team name</label><input className="mt-2 w-full rounded-lg border p-3" value={name} onChange={(e)=>setName(e.target.value)} required/><button className="mt-6 w-full rounded-lg bg-slate-900 p-3 font-semibold text-white disabled:opacity-50" disabled={loading}>{loading?'Creating...':'Create Team'}</button></form></main>}
